services:
  argus:
    build: .
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env
      - ./data:/app/data
      - ./logs:/app/logs
      - ./sessions:/app/sessions
    stdin_open: true
    tty: true
    restart: unless-stopped
